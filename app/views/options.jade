form.form-horizontal(ng-submit="save()")
  .control-group
    .controls
      label.checkbox
        input(type="checkbox", ng-model="autostart")
        |  Autostart Robot when you start app
  .form-actions
    button.btn.btn-primary(type='submit') Apply
    button.btn(type='button', ng-click='cancel()') Cancel
